import React from "react";
import ReactDOM from "react-dom/client";
import AppQuiz from "./AppQuiz.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppQuiz />
  </React.StrictMode>
);
